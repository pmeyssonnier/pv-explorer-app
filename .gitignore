# ═══════════════════════════════════════════════════════════════
# SECRETS — NE JAMAIS COMMITTER (crucial !)
# ═══════════════════════════════════════════════════════════════
.env
.env.*
!.env.example
*.key
secrets.json
credentials.json

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# ── OS / éditeurs ──
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ── Logs & temporaires ──
*.log
*.tmp
tmp/

# ── Pinecone / cache local éventuel ──
cache/
backups/

# ── Données sources lourdes — JAMAIS dans le repo (PDF des PV, dossier local) ──
# Le corpus PDF (~100+ Mo) reste sur ta machine / Drive ; seul le JSON extrait
# (pv_conseil_schaerbeek.json) est suivi.
PV/
*.pdf
# Copies de travail accidentelles de la base (« pv_conseil_schaerbeek (1).json »)
* (*).json
